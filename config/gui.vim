scriptencoding utf-8

let &guifont=hz#ui#set_guifont()

" No menu completion with ALT+Letter. While this doesn't apply to the Mac, it
" applies to more than one other GUI configuration, so common.
set winaltkeys=no

" Mouse settings
set nomousefocus mousehide

" Menu options, where applicable.
set guioptions=gmec
