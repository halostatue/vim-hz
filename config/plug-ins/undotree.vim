scriptencoding utf-8

if hz#plugins#has('undotree')
  let g:undotree_WindowLayout = 3 " right-hand, tree over diff
  let g:undotree_ShortIndicators = 1 " use d instead of days
  let g:undotree_SetFocusWhenToggler = 0
endif
