scriptencoding utf-8

set fuoptions=maxvert,maxhorz
set macligatures

if 0 && has('transparency')
  set transparency=25 blurradius=25
endif

" set columnspace=-1
" set macmeta

" Put the window on the right side of my MBP 13" screen, half-width and full
" height.
set lines=62
set columns=106
winpos 641 22
